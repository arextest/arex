import { styled } from 'arex-core';

const FlexRowReverseWrapper = styled.div`
  display: flex;
  flex-direction: row-reverse;
  .ant-btn-primary {
    margin-bottom: 8px;
  }
`;

export default FlexRowReverseWrapper;
