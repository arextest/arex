import { FC } from 'react';
interface ReplayMenuProps {
  value: string;
  onSelect: () => void;
}
const ReplayMenu: FC<ReplayMenuProps> = () => {
  return <div>ReplayMenu</div>;
};

export default ReplayMenu;
