export enum Theme {
  light = "light",
  dark = "dark",
}
export enum ThemeIcon {
  light = "🌞",
  dark = "🌛",
}

export const DefaultTheme = Theme.light;

export const Color = {
  primaryColor: "#603BE3",
};
